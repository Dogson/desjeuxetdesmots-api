import {DEFAULT_QUERY_VALUES, DefaultQuery} from "../../shared/const/default.query";

export class GameQuery extends DefaultQuery {
}

export const DEFAULT_GAME_QUERY: GameQuery = {
    ...DEFAULT_QUERY_VALUES
};